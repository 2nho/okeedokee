<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- 각 게시판의 셀렉문을 구해서 합치기.. -->
<mapper namespace="bbs">
	<!-- 신고게시판 -->
	<select id="selectRBBS" parameterType="adminRVO" resultType="adminRVO">
		SELECT 
		FROM 
		WHERE 
	</select>
</mapper>